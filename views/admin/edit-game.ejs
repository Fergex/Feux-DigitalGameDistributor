<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/game.css">
</head>

<body>
   <%- include('../includes/navigation.ejs') %>

    <main>
        <form class="game-form" action="/admin/<% if (editing) { %>edit-game<% } else { %>add-game<% } %>" method="POST">
            <div class="form-control">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" value="<% if (editing) { %><%= game.title %><% } %>">
            </div>
            <div class="form-control">
                <label for="imageUrl">Image URL</label>
                <input type="text" name="imageUrl" id="imageUrl" value="<% if (editing) { %><%= game.imageUrl %><% } %>">
            </div>
            <div class="form-control">
                <label for="price">Price</label>
                <input type="number" name="price" id="price" step="0.01" value="<% if (editing) { %><%= game.price %><% } %>">
            </div>
            <div class="form-control">
                <label for="description">Description</label>
                <textarea name="description" id="description" rows="5"><% if (editing) { %><%= game.description %><% } %></textarea>
            </div>
            <% if (editing) { %>
                <input type="hidden" value="<%= game.id %>" name="gameId">
            <% } %>

            <button class="btn" type="submit"><% if (editing) { %>Update Game<% } else { %>Add Game<% } %></button>
        </form>
    </main>
<%- include('../includes/end.ejs') %>